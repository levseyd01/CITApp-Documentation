selector_to_html = {"a[href=\"#developer-dependencies\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Developer Dependencies<a class=\"headerlink\" href=\"#developer-dependencies\" title=\"Link to this heading\">#</a></h1><p>A JavaScript runtime is required for building the library.</p><p>Run the tests (using PhantomJS):</p>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(`article.bd-article ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false,

            });
        };
    };
    console.log("tippy tips loaded!");
};
